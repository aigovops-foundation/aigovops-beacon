node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store

# beacon-v2 CLI — local state + secrets, never commit
.beacon-v2-state.json
.beacon-v2.env

# local sqlite used by tests + `npm run dev` (the Fly volume is /data)
.data/
*.db
*.db-shm
*.db-wal
